# ============== Backend PRODUKSI (deploy ke Google AI Studio) ==============
# SENGAJA TANPA torch/torchvision/timm -- itu penyebab app > 600MB.
# Model dijalankan lewat onnxruntime (jauh lebih kecil), file .onnx-nya
# didownload dari Hugging Face Hub saat startup (lihat src/inference.py).
# SENGAJA TANPA jinja2/authlib/itsdangerous -- backend ini API murni (JSON),
# tampilan ada di frontend/ terpisah (deploy ke GitHub Pages).

# --- Web framework & server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # wajib buat FastAPI terima file upload (UploadFile)
python-dotenv>=1.0.0

# --- Auth (JWT + verifikasi ID token Google, BUKAN cookie session/OAuth redirect) ---
pyjwt>=2.8.0
google-auth>=2.30.0
requests>=2.31.0               # dipakai google-auth buat verifikasi token ke server Google

# --- AI text explanation ---
google-generativeai>=0.5.0     # opsional -- fallback offline kalau kosong

# --- Laporan PDF ---
reportlab>=4.0.0

# --- Inference model (ONNX, bukan torch) ---
onnxruntime>=1.18.0
huggingface_hub>=0.24.0

# --- Preprocessing gambar (tanpa torch) ---
opencv-python-headless>=4.8.0
albumentations>=1.3.0
Pillow>=10.0.0
numpy>=1.24.0

# --- Database (Turso di produksi, fallback SQLite lokal saat development) ---
libsql>=0.1.0
